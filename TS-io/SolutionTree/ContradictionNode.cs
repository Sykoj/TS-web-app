namespace Ts.IO {

    /// <summary>
    /// Leaf of tableau solution tree of tableau branch which is finished and contradictionary
    /// </summary>
    public class ContradictionNode : SolutionNode {
    }
}
