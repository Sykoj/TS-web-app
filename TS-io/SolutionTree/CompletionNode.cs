namespace Ts.IO {

    /// <summary>
    /// Leaf of tableau solution tree of tableau branch which is finished and nonctradictionary
    /// </summary>
    public class CompletionNode : SolutionNode {
    }
}
